# Default carry when a mission doesn't specify `weapon = <id>` (see
# Game::init). Same numbers Weapon's own member defaults used before the
# weapon system existed, so every pre-existing mission plays unchanged.
name = Standard Rifle
damage = 22
headshot_multiplier = 2.0
mag_size = 24
reserve_ammo = 96
fire_interval = 0.11
reload_time = 1.6
