# Not a real mission — a one-hostile fixture for verifying the shoot/kill/
# mission-complete pipeline in isolation, without a squad's combined DPS
# confounding the test. Real content lives in the other files in this
# directory; this one stays because it's a useful smoke test for whoever
# touches Weapon/Hostile/Game next.
name = DEBUG: single target
arena = 80

wave scarab 1 12
